/**
 * SimGrid GridOS API client — stub.
 * Phase 0 spike required: confirm auth mechanism, available scopes,
 * rate limits, and webhook vs. poll strategy before implementing.
 * See docs/PROJECT_BRIEF.md § "Open decision to confirm before running"
 */
export const VERSION = '0.0.0'; // stub — Phase 0 spike required

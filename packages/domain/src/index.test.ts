import { describe, it, expect } from 'vitest';
import { computeStandings } from './index.js';

describe('computeStandings', () => {
  it('is a stub that throws until implemented', () => {
    expect(() => computeStandings()).toThrow('not implemented');
  });
});

/**
 * SRA store package — durable persistence layer.
 * Owns: driver/team mappings (SimGrid IDs ↔ internal IDs),
 * manual overrides, result snapshots, and the penalty ledger audit log.
 * See docs/adr/0003-snapshot-based-auditing.md
 */
export const VERSION = '0.0.0';

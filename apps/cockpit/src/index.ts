/**
 * SRA Cockpit application — stub.
 * Frontend race-director tool. Renders only — never computes standings.
 * NOT the public SRA marketing site (that lives separately).
 */
export const VERSION = '0.0.0';

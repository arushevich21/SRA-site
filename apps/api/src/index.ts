/**
 * SRA API application — stub.
 * Thin backend: exposes domain logic via stable REST endpoints.
 * Endpoints: /dashboard, /teams, /standings/audit, /rounds/:id/ops, /announcements
 */
export const VERSION = '0.0.0';

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Test coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# pnpm
.pnpm-store/

# Spike / investigation scripts — never production code
scratch/

# Raw API fixtures — may contain sensitive league data; regenerate from the spike
fixtures/gridos/
.next/
*Zone.Identifier
scripts/drivers_seed.json

# Supabase CLI's local link cache (project ref, pooler URL, version info) —
# machine-local state from `supabase link`, not project source. schema.sql
# (the actual schema snapshot dump-schema.ps1 produces) is committed.
supabase/.temp/